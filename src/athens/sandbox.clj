(ns athens.sandbox)

(defn message []
  "Welcome, dear Athenian!")

;; Connect to your REPL and try to run this block straight from your editor!
(message)

;; Now, rewrite the `message` function, run the (defn ...) block, and rerun
;; the (message) block.

;; Did it work?

