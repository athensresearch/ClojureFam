(ns athens.sandbox)

(defn message []
  "Welcome, dear Athenian!")

;; Try to run this block straight from your editor!
(message)

;; Now, rewrite the `message` function, run the (defn ...) block, and rerun
;; the (message) block.

;; Did it work?
